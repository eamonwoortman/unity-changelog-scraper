# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python application

on:
  push:
    branches: [ "master", "develop" ]
  pull_request:
    branches: [ "master", "develop" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
#    - name: Test with pytest
#      run: |
#        pytest
    - name: Set build arguments
      if: github.ref == 'refs/heads/master'
      run: |
        echo "::set-env name=BUILD_ARGS::--output-folder github-pages"
    - name: Set build arguments
      if: github.ref != 'refs/heads/master'
      run: |
        echo "::set-env name=BUILD_ARGS::--full-set False --output-folder github-pages "
    - name: Build
      run: python unity_changelog_scraper.py $BUILD_ARGS 
#    - name: Upload GitHub Pages artifact
#      uses: actions/upload-pages-artifact@v1.0.7
#      with:
#        # Artifact name
#        name: github-pages # optional, default is github-pages
#        # Path of the directory containing the static assets.
#        path: output # default is _site/
#        # Duration after which artifact will expire in days.
#        retention-days: 1 # optional, default is 1      
